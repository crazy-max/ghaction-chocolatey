name: test

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  choco:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v1
      -
        name: Choco help
        uses: ./
        with:
          args: -h
